let age = 13;

let maxAge;
maxAge = 100; 

let numberperday;
numberperday = 2;

let totalRequired
totalRequired = (numberperday*365)*(maxAge - age)

let result = "You will need " + totalRequired + " packs of chips to last you until the ripe old age of " + maxAge + ".";
document.write(result)